server:
  port: 8082
  servlet:
    context-path: /

spring:
  application:
    name: content-service

  datasource:
    url: jdbc:mysql://localhost:3306/heartmatch_content?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.heartmatch: DEBUG